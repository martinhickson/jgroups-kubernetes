<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Standalone module: not part of the root reactor -->
    <groupId>org.jgroups.kubernetes</groupId>
    <artifactId>kubernetes-it</artifactId>
    <version>1.0.17.Final</version>
    <packaging>jar</packaging>
    <name>kubernetes-it</name>
    <description>Failsafe integration tests: 3-pod JGroups + KUBE_PING against kind</description>

    <properties>
        <jgroups-kubernetes.version>1.0.17.Final</jgroups-kubernetes.version>
        <version.jgroups>4.2.4.Final</version.jgroups>
        <version.junit>4.13.2</version.junit>
        <version.assertj>3.25.3</version.assertj>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <kubernetes.it.cluster>kubernetes-it</kubernetes.it.cluster>
        <kubernetes.it.namespace>kubernetes-it</kubernetes.it.namespace>
        <kubernetes.it.image>kubernetes-it-jgroups-member:it</kubernetes.it.image>
        <kubernetes.it.reuse.cluster>false</kubernetes.it.reuse.cluster>
        <kubernetes.it.skip.teardown>false</kubernetes.it.skip.teardown>
        <kubernetes.it.token.refresh.seconds>10</kubernetes.it.token.refresh.seconds>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.jgroups.kubernetes</groupId>
            <artifactId>jgroups-kubernetes</artifactId>
            <version>${jgroups-kubernetes.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jgroups</groupId>
            <artifactId>jgroups</artifactId>
            <version>${version.jgroups}</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${version.junit}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${version.assertj}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>kubernetes-it-member</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>org.jgroups.kubernetes.it.member.JGroupsMemberMain</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <includes>
                        <include>**/*IT.java</include>
                    </includes>
                    <redirectTestOutputToFile>false</redirectTestOutputToFile>
                    <trimStackTrace>false</trimStackTrace>
                    <systemPropertyVariables>
                        <kubernetes.it.project.root>${project.basedir}</kubernetes.it.project.root>
                        <kubernetes.it.cluster>${kubernetes.it.cluster}</kubernetes.it.cluster>
                        <kubernetes.it.namespace>${kubernetes.it.namespace}</kubernetes.it.namespace>
                        <kubernetes.it.image>${kubernetes.it.image}</kubernetes.it.image>
                        <kubernetes.it.reuse.cluster>${kubernetes.it.reuse.cluster}</kubernetes.it.reuse.cluster>
                        <kubernetes.it.skip.teardown>${kubernetes.it.skip.teardown}</kubernetes.it.skip.teardown>
                        <kubernetes.it.token.refresh.seconds>${kubernetes.it.token.refresh.seconds}</kubernetes.it.token.refresh.seconds>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
